

const Th = ({name=""}) => {
    return(
        <th className="p-3 font-bold uppercase bg-gray-200 text-gray-500 border border-gray-300 hidden lg:table-cell capitalize">{name}</th>
    )
};

export default Th